const AuthModal = () => {
  const handleClick = () => {
    setShowModal(false);
  };

  return (
    <div>
      <div className="" onClick={handleClick}></div>
    </div>
  );
};

export default AuthModal;
